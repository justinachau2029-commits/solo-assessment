# solo-assessment
If i am collaborating with a classmate the first thing that each of my team members need to do is to be on the same branch as me. This step is important because if my teammates are not on the same branch as me it will fail if we start to save.
Developers use branches because it would be more useful and helpful
Git is a Distributed version control system that has access to all changes 
Github is a cloud based service that allows developers to store,track,manage and control changes to their code 
The correct command sequence should be 
git and github works together because they are both networks that have similar contributes to eachother
